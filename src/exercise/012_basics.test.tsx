// import { fireEvent, render, screen } from '@testing-library/react';
import { BasicsFunc } from './011_basics';

/**
 * Uncomment @testing-library/react imports
 * Write a test for BasicsFunc component
 * Render it
 * Increment the counter twice and then decrement once
 *
 * Related documentation:
 * https://testing-library.com/docs/react-testing-library/cheatsheet
 */

test('it should increment and decrement the counter', () => {
  expect(BasicsFunc).toBeDefined();
});
